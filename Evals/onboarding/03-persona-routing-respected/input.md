# Input

Run onboarding against `inputs/jordan-lee-profile.md` — Jordan chose **Executive operator**.

Grade the resulting `CLAUDE.md`, the suggested first commands, and the assistant's routing language in the final summary — checking the chosen persona's row is applied, not a heavier persona's defaults.
