# Results — peer-review suite

Run result files (`YYYY-MM-DD_<model>.md`) and captured transcripts (`transcripts/`) land here.

Per repo convention these are gitignored in the public template (`Evals/*/results/*`); private working copies should commit or otherwise persist run evidence.
