# Knowledge/Ingestion — staging area

Raw artifacts land here before promotion. The agent proposes; the PM decides.

- Written by `/wiki-ingest` (staging notes for each new source).
- Swept by `/wiki-maintain` Check 8 — more than 10 unprocessed items here is flagged 🔴 as ingestion backlog.
- Items leave this folder by being promoted into `Knowledge/` proper (with a provenance tag) or archived. Nothing lives here long-term.
